version: "3.8"

services:
  pkm-server:
    image: ghcr.io/eviljoker/pkm:snapshot
    container_name: pkm-server
    ports:
      - "8890:7890"
    volumes:
      - ~/.pkm:/root/.pkm
    restart: unless-stopped
    env_file:
      - ~/.pkm/.env
